# archive/ — Material legacy (no borrar sin revisar)

| Subcarpeta | Contenido |
|---|---|
| `legacy/` | `MISISTEMAV1.xlsm` (sistema VBA Excel origen baremos), `build_installer.py` obsoleto |
| `scripts-oneoff/` | Scripts one-off de fixes frontend (may 2026) |

**No ejecutar** scripts aquí sin leer el código — pueden estar desactualizados.

Para build actual: `build.py` + `installer/NeuroSoft.iss` (skill `/build-beta-tester`).
