# requirements-dev.txt — NeuroSoft Backend (desarrollo)
# =======================================================
# Dependencias adicionales para desarrollo, testing y CI.
# Incluyen las de producción: `-r requirements.txt`.
#
# Instalar:  pip install -r requirements-dev.txt
# CI:        el pipeline usa este archivo para ejecutar tests.

-r requirements.txt

# ── Testing ───────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-cov==5.0.0
pytest-mock>=3.12.0
httpx==0.27.0             # Cliente HTTP para tests de endpoints FastAPI

# ── Calidad de código ─────────────────────────────────────
ruff>=0.4.0               # Linter + formatter (reemplaza flake8+black+isort)
mypy>=1.10.0              # Type checker estático
types-python-dateutil     # Stubs para mypy
