# requirements-desktop.txt — dependencias extra para el modo escritorio
# (ventana nativa pywebview + empaquetado PyInstaller)
#
# Instalar:
#     pip install -r requirements-desktop.txt
#
# El resto de dependencias del backend viven en neurosoft-backend/requirements.txt

# Ventana nativa — usa WebView2 (Edge) en Windows, WebKit en macOS, GTK en Linux
pywebview>=5.0
pythonnet>=3.0

# Empaquetado a .exe
pyinstaller>=6.6

# Exportación a Excel (hoja de puntajes)
openpyxl>=3.1.0

# Exportación a Word (informe editable)
python-docx>=1.1.0
