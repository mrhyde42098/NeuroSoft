# Extracciones de protocolos Pearson/WAIS

**No versionar en GitHub.** Contienen texto y criterios de scoring del manual
licenciado (copyright Pearson / Manual Moderno).

Regenerar solo en entorno local con licencia válida:

```powershell
python docs/scripts/extract_protocol_pdfs.py
```

Los `.txt` de esta carpeta están en `.gitignore`.
